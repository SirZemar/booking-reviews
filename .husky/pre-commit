# Styles
RED='\033[0;31m'
GREEN='\033[0;32m'
BOLD='\033[1m'
RESET='\033[0m'

npm run lint || { echo "${RED}${BOLD}Commit aborted:${RESET} ${BOLD}Linting failed.${RESET}"; false; }
echo "${GREEN}${BOLD}Pre-commit success:${RESET} ${BOLD}Linting success.${RESET}" || true
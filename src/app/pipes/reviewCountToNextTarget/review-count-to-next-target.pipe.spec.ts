import { ReviewCountToNextTargetPipe } from './review-count-to-next-target.pipe';

describe('ReviewCountToNextTargetPipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewCountToNextTargetPipe();
    expect(pipe).toBeTruthy();
  });
});

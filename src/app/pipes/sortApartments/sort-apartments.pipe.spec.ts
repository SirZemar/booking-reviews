import { SortApartmentsPipe } from './sort-apartments.pipe';

describe('SortApartmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortApartmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
